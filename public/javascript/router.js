define([
    'jquery',
    'underscore',
    'backbone'
    ], function($, _, Backbone) {
   
        Backbone.history.start();
        
        var initialize = function(){};
        
        return {
            
            initialize: initialize
        };
    
});